ACCESS_TOKEN=your_discord_bot_token

DB_HOST=
DB_PORT=5432
DB_USER=precut
DB_PASSWORD=change_me
DB_NAME=precut

SCHEMA_PATH=src/schema.sql
PREVIEWS_DIR=./data/previews

# cuda, cpu, or mps — leave empty to auto-detect (uses CUDA on your 4070 Ti Super)
EMBEDDING_DEVICE=
EMBEDDING_BATCH_SIZE=64
